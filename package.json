{
  "name": "99-pai-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:mobile": "npm run start --workspace=@99-pai/mobile",
    "build:web": "npm run build:web --workspace=@99-pai/mobile"
  }
}
